<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.idanbeta">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Profiles"></activity>
        <activity android:name=".Sheetfeedback" />
        <activity android:name=".Passing" />
        <activity android:name=".Addnew" />
        <activity android:name=".Clientpage" />
        <activity android:name=".Patientlists" />
        <activity android:name=".Physiolists" />
        <activity android:name=".Lognreg" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>