<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.idanbeta">

    <uses-permission android:name="android.permission.SEND_SMS"/>

    <uses-permission android:name="android.permission.RECEIVE_SMS"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_foreground"
        android:label="MyTrainer"
        tools:replace="android:label"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Credits"></activity>
        <activity android:name=".ExList" />
        <activity android:name=".NewEx" />
        <activity android:name=".Profiles" />
        <activity android:name=".Sheetfeedback" />
        <activity android:name=".Passing" />
        <activity android:name=".Addnew" />
        <activity android:name=".Clientpage" />
        <activity android:name=".ClientMain" />
        <activity android:name=".TrainerMain" />
        <activity android:name=".LoginRegister" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>